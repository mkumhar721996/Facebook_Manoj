{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types src/index.ts",
    "test": "SESSION_SECRET=test-session-secret node --experimental-strip-types --test \"src/**/*.test.ts\""
  }
}
